import React from 'react';
import { Text } from 'react-native';
import styles from './styles';

const Title = () => {
    return (
        <Text style={styles.tictle}>Hello</Text>
    );
};

export default Title;
